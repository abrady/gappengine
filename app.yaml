application: drinkbot9000 
version: 1
runtime: python
api_version: 1

handlers:

# run script named same as directory, 
# e.g. http://localhost/foo/ runs foo/foo.py
- url: /(.*?)/.*
  script: \1/\1.py

- url: /.*
  script: home.py